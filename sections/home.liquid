{% liquid
    # Get the featured collection
    assign featured_collection = collections['featured']
    assign latest_collection = collections['latest']

    # Check if collection exists and has products
    assign has_featured = false
    if featured_collection != blank and featured_collection.products.size > 0
        assign has_featured = true
    endif

    assign has_latest = false
    if latest_collection != blank and latest_collection.products.size > 0
        assign has_latest = true
    endif
%}

<div class="px-4 space-y-6">

    {% if section.settings.show_latest and has_latest %}
         <!-- Products Grid -->
        <div class="grid grid-cols-1 lg:grid-cols-2  gap-6">
            {% for product in latest_collection.products limit: 12 %}
                {% liquid
                    assign product_image = product.featured_image
                    assign image_url = product_image | image_url: width: 400, height: 200, crop: 'center'
                    assign image_alt = product.title | escape
                %}

                <article
                        class="group relative bg-white rounded-xl overflow-hidden shadow-sm hover:shadow-lg transition-all duration-300 hover:-translate-y-1">
                    <a href="{{ product.url }}"
                       class="block w-full h-full focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">
                        <!-- Product Image -->
                        <div class="aspect-square overflow-hidden">
                            {% if product_image %}
                                <img
                                        src="{{ image_url }}"
                                        width="400"
                                        height="400"
                                        alt="{{ image_alt }}"
                                        class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
                                        loading="lazy"
                                        decoding="async"
                                >
                            {% else %}
                                <div class="w-full h-full bg-gray-100 flex items-center justify-center">
                                    <svg class="w-16 h-16 text-gray-400" fill="none" stroke="currentColor"
                                         viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1"
                                              d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                                    </svg>
                                </div>
                            {% endif %}
                        </div>

                        <!-- Product Overlay -->
                        <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-black/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                            <div class="absolute bottom-0 left-0 right-0 p-4">
                                <h3 class="text-white text-lg font-semibold leading-tight line-clamp-2">
                                    {{ product.title | escape }}
                                </h3>

                                <!-- Price -->
                                <div class="mt-2">
                  <span class="text-white font-bold text-lg">
                    {{ product.price | money }}
                  </span>
                                    {% if product.compare_at_price > product.price %}
                                        <span class="text-gray-200 line-through ml-2">
                      {{ product.compare_at_price | money }}
                    </span>
                                    {% endif %}
                                </div>
                            </div>
                        </div>
                    </a>
                </article>
            {% endfor %}
        </div>

    {%  endif %}

    {% if section.settings.show_featured and has_featured %}
        <!-- Products Grid -->
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
            {% for product in featured_collection.products limit: 12 %}
                {% liquid
                    assign product_image = product.featured_image
                    assign image_url = product_image | image_url: width: 400, height: 400, crop: 'center'
                    assign image_alt = product.title | escape
                %}

                <article
                        class="group relative bg-white rounded-xl overflow-hidden shadow-sm hover:shadow-lg transition-all duration-300 hover:-translate-y-1">
                    <a href="{{ product.url }}"
                       class="block w-full h-full focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">
                        <!-- Product Image -->
                        <div class="aspect-square overflow-hidden">
                            {% if product_image %}
                                <img
                                        src="{{ image_url }}"
                                        width="400"
                                        height="400"
                                        alt="{{ image_alt }}"
                                        class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
                                        loading="lazy"
                                        decoding="async"
                                >
                            {% else %}
                                <div class="w-full h-full bg-gray-100 flex items-center justify-center">
                                    <svg class="w-16 h-16 text-gray-400" fill="none" stroke="currentColor"
                                         viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1"
                                              d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                                    </svg>
                                </div>
                            {% endif %}
                        </div>

                        <!-- Product Overlay -->
                        <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-black/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                            <div class="absolute bottom-0 left-0 right-0 p-4">
                                <h3 class="text-white text-lg font-semibold leading-tight line-clamp-2">
                                    {{ product.title | escape }}
                                </h3>

                                <!-- Price -->
                                <div class="mt-2">
                  <span class="text-white font-bold text-lg">
                    {{ product.price | money }}
                  </span>
                                    {% if product.compare_at_price > product.price %}
                                        <span class="text-gray-200 line-through ml-2">
                      {{ product.compare_at_price | money }}
                    </span>
                                    {% endif %}
                                </div>
                            </div>
                        </div>
                    </a>
                </article>
            {% endfor %}
        </div>
    {% endif %}
</div>

{% schema %}
{
  "name": "Home",
  "tag": "section",
  "class": "home",
  "settings": [
    {
      "type": "checkbox",
      "id": "show_latest",
      "label": "Show Latest",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_featured",
      "label": "Show Featured",
      "default": true
    }
  ],
  "presets": [
    {
      "name": "Home"
    }
  ]
}
{% endschema %}
